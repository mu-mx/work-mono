
import { Outlet } from "react-router-dom";
import { Layout } from "antd";
import AutoNavigate from "@/components/AutoNavigate";
import React from "react";

export default function BasicLayout(props: any) {
  console.log("props -> :", props);
  return (
    <>
      1165161
      {/*       
      <AutoNavigate />
      <Layout.Sider style={{ background: "skyblue" }}>side bar</Layout.Sider>
      <Layout>
        <Layout.Content>
          <Outlet />
        </Layout.Content>
        <Layout.Footer>^(^(&**)_)</Layout.Footer>
      </Layout> */}
    </>
  );
}
